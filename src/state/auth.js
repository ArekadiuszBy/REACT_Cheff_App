const initialState = {
    isLogged: false  //na początku nie jesteśmy zalogowani
}

export default (state = initialState, action) => {
    switch (action.type){
        default:
            return state
    }
}